--extra-index-url https://download.pytorch.org/whl/cu124

# Python >= 3.11 is required: wigglystuff 0.5.15+ declares it in Requires-Python,
# so pip refuses to install it under 3.10. The notebook's runtime installer runs
# that pip call with `subprocess.run(..., check=True)`, so a 3.10 kernel raises at
# the setup cell rather than degrading to a notebook without the widgets.

# Model + multimodal input
qwen_omni_utils==0.0.9
# qwen-omni-utils 0.0.9 imports `audioread` at module scope (and uses it, via
# audioread.ffdec) without declaring it, and declares `librosa` — which is
# absent on any environment whose dependency set was resolved differently.
# Without both, `from qwen_omni_utils import process_mm_info` raises
# ModuleNotFoundError.
#
# librosa stays unpinned on purpose. 1.0.0 (Python >= 3.12, so molab gets it)
# dropped the audioread reader that qwen-omni-utils still feeds — pinning back to
# 0.11 would force a downgrade and a numba/soxr re-resolve on every 3.12+ kernel,
# so the notebook's setup cell restores that reader instead.
audioread
librosa
accelerate==1.14.0
torch==2.6.0+cu124
torchvision==0.21.0+cu124
# The repository's historical 4.52.0.dev0 build is no longer published.
transformers==4.52.0

# Notebook runtime. The molab notebook installs these itself (molab ignores the
# `# /// script` block), but a local `uv pip install -r requirements.txt` needs
# them listed or the notebook will not open.
marimo
matplotlib
numpy
# PyAV backs the `torchvision.io.read_video` shim: torchvision >= 0.23 dropped
# the built-in video decoder, and qwen-omni-utils still calls it.
av

# Classroom widgets. `wigglystuff` supplies TextCompare and TangleSlider;
# `anywidget` is listed explicitly because `src/probe_grid.py` is a first-party
# widget and should not depend on another package's dependency graph.
wigglystuff==0.5.21
anywidget>=0.9.2
